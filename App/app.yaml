apiVersion: apps/v1
kind: depoyment
metadata:
  name: simple-web-app-deploy-d889cbdbf-zr2hl
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      bb: web
  template:
    metadata:
      labels:
        bb: web
    spec:
      containers:
      - name: app
        image: simple-web-app:latest
---
apiVersion: v1
kind: Service
metadata:
  name: simple-web-app-deploy
  namespace: default
spec:
  type: NodePort
  selector:
    bb: web
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 32411

    